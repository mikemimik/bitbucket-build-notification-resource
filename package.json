{
  "name": "build-notification-resource",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "docker build --tag mperrotte/bitbucket-build-notification-resource .",
    "push": "docker push mperrotte/bitbucket-build-notification-resource:latest",
    "deploy": "npm run build && npm run push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sync-request": "^5.0.0"
  },
  "devDependencies": {
    "semistandard": "^12.0.1"
  }
}
